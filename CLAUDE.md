## ibkr-mcp

Read-only MCP server for Interactive Brokers. Registered as HTTP at `http://127.0.0.1:3099/mcp`.

Before using `mcp__ibkr__*` tools, start the server if it's not running:

```sh
ibkr-mcp
```

Requires TWS or IB Gateway on 127.0.0.1:4002.
